version: '2'
services:
  web:
    build: .
    ports:
     - "3000:3000"
    volumes:
     - .:/usr/src/app
    depends_on:
     - "redis"
  redis:
    image: "redis:alpine"
networks:
  redis:
    driver: bridge